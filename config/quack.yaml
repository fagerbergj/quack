# Quack configuration. Structure is declarative; ${VAR} are interpolated from
# the environment (secrets never live in this file). See docs/configuration.md.
providers:
  default:
    kind: openai            # API protocol; the endpoint picks the actual server
    endpoint: ${LLM_ENDPOINT}
    api_key: ${LLM_API_KEY}

stores:
  relational:
    kind: postgres
    url: ${DATABASE_URL}

# Semantic memory (M6). Presence of this block enables it; if QDRANT_URL is unset
# the block self-disables (recall/commit off) so qdrant-less runs still work.
# Recall is ambient (preload_memory) + deliberate (load_memory). Embeddings use
# the always-warm qwen3-embed group.
memory:
  url: ${QDRANT_URL}
  embedder:
    provider: default
    model: ${EMBED_MODEL}
  top_k: 5

orchestrator:
  provider: default
  model: ${ORCH_MODEL}          # the thin dispatcher: routes requests to agents

# Declarative agents. Each is an agent-card.json + prompt.md bundle on disk,
# bound here to a model and a selection of built-in tools. Add an agent by
# dropping a bundle directory and adding an entry — no recompile.
agents:
  web-researcher:
    bundle: agents/web-researcher
    provider: default
    model: ${RESEARCHER_MODEL}
    context_window: 65536       # qwen3.6-35b served -c 131072 --parallel 2 ⇒ 65536/slot (one node = one slot)
    tools: [web_search, web_fetch, summarize, current_date, load_memory]
  synthesizer:
    bundle: agents/synthesizer
    provider: default
    model: ${RESEARCHER_MODEL}
    context_window: 65536       # same model as web-researcher
    tools: []
  media-reader:
    bundle: agents/media-reader
    provider: default
    model: ${MEDIA_MODEL}       # Qwen3-Omni (media llm-swap group); falls back to RESEARCHER_MODEL if unset
    context_window: 32768       # qwen3-omni-30b served -c 32768 --parallel 1
    tools: []
    inputs: [text, image, audio]
  image-reader:
    bundle: agents/image-reader
    provider: default
    model: ${IMAGE_MODEL}       # Qwen3-VL-32B (vision llm-swap group); specialist for hard image reading
    context_window: 32768       # qwen3-vl-32b served -c 32768
    tools: []
    inputs: [text, image]

# Backend bindings for the built-in tools (keyless, internal services in M1).
tools:
  web_search:
    backend: ${SEARXNG_URL}        # SearXNG JSON API, e.g. http://searxng:8080
  web_fetch:
    render_backend: ${CRAWL4AI_URL}  # crawl4ai /md backend, e.g. http://crawl4ai:11235

# The trust gate wraps every agent and runs three stages cheapest-first, each
# with its own round budget (a stage with max_rounds:0 is skipped):
#   1. deterministic_checks — free code checks (citation backing + length) that
#      drive cheap targeted revisions to drop fabricated citations up front.
#   2. self_critique — the worker critiques and revises its own draft.
#   3. judge — an independent model scores the answer; the worker revises on fail.
# Deterministic citation/length scores are always folded into the judge's verdict.
# Leave judge.model empty to disable the judge (the cheaper stages still run).
gates:
  constitution_path: config/constitution.md   # global principles, used for self-critique + judge
  rubric_path: config/rubric.md               # default scoring guide (agents may override with rubric.md in their bundle)
  deterministic_checks:
    max_rounds: 4   # free citation/length checks; up to 4 cheap worker revise cycles
  self_critique:
    max_rounds: 1   # worker self-improvement passes before the judge
  judge:
    provider: default
    model: ${JUDGE_MODEL}   # empty ⇒ judge disabled
    max_rounds: 1           # expensive model-judge/revise rounds
    threshold: 0.7          # PER-CRITERION pass bar: every rubric criterion must clear it.
                            # On the rubric's 0-10 scale this is 7/10 ("met, with only
                            # minor gaps"); the verdict score is the lowest criterion
                            # (weakest-link gating, no averaging, no caps).
    max_iterations: 6       # safety cap on the judge's model turns per round

# DAG execution policy.
dag:
  max_active_nodes: 2   # how many nodes run concurrently; extras queue. Matched to the
                        # worker server's --parallel 2 (2 KV slots @ 65536 tokens each);
                        # the GPU judge (gemma4-26b-a4b, co-resident with the worker)
                        # is also --parallel 2. Raising this past the worker's slot
                        # count just queues requests at the model.

# Automatic context compaction (ports sst/opencode's prune + compaction). When
# enabled, every gated agent gets a callback that, before each model call, blanks
# old tool outputs and — if the turn's measured usage exceeds the agent's
# per-agent context_window (set above) minus the output reserve — summarises the
# older turns via the summariser model. Unset ${COMPACTION_ENABLED} ⇒ null ⇒ false.
compaction:
  enabled: ${COMPACTION_ENABLED}
  provider: default
  # Summariser model (required when enabled). Pick one CO-RESIDENT with the
  # workers so a mid-run summary doesn't evict+reload a worker: the judge
  # (gemma4-26b-a4b) is always warm alongside every worker (llm-swap set
  # "(g|q|v) & j"), so it's the cheap default. COMPACTION_MODEL defaults to it.
  model: ${COMPACTION_MODEL}
  prune: true                  # blank old tool outputs before summarising (cheap, no model call)

server:
  addr: ":8080"
