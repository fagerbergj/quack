{
  "nodes": [
    {
      "id": "extract",
      "agent": "{{extract_agent}}",
      "task": "Transcribe or OCR the attached file faithfully and structure it as clean, well-formed Markdown (headings, lists, and tables where they fit). Do not summarize or omit content. Output only the Markdown.",
      "depends_on": []
    },
    {
      "id": "cleanup",
      "agent": "general-purpose",
      "task": "Clean up and clarify the extracted document provided above: resolve cross-references, fix inconsistencies, and tighten without dropping any entity, claim, or decision. If a reference is unclear, check the stored corpus with your search tools. Output only the cleaned Markdown.",
      "depends_on": ["extract"]
    },
    {
      "id": "classify",
      "agent": "classifier",
      "task": "Classify the cleaned document provided above. Emit ONLY the strict JSON object with tags, summary, and confidence.",
      "depends_on": ["cleanup"]
    },
    {
      "id": "persist",
      "agent": "document-writer",
      "task": "Persist the document. The cleaned Markdown content and the classification JSON (tags + summary) are in the inputs above. Call create_document with content set to the cleaned Markdown, a concise derived title, summary and tags from the classification, and content_hash set to \"{{content_hash}}\" exactly (so a re-run dedups). Output the resulting document id.",
      "depends_on": ["cleanup", "classify"]
    }
  ]
}
